#include <iostream>
#include <cmath>
using namespace std;

int main() {
	int N;
	cin >> N;
	for (int i = 0; i < N; i++) {
		long long ll;
		cin >> ll;
		long long biggest = 0;
		while (ll--) {
			int exp = 1;
			while (ll > 10) {
				ll = ll / 10;
				exp++;
			}

		}
	}
}